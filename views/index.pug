extends layout

block content
  h1= title
  p The purpose of this application is to become familiar with using pug templates to dynamically generate web pages on the server side.
  button(type="button", class="btn btn-primary" onClick="method()") Press Me
  div#target


  script.
    var client = 0
    function method(){
      client++
      console.log(client)
      document.getElementById("target").innerHTML = "Value now " +client;
    }